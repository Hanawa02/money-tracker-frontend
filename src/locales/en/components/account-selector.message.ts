import AccountSelector from "~/locales/interfaces/components/account-selector.interface";

const accountSelector: AccountSelector = {
  noneSelected: "Select an account",
};

export default accountSelector;
