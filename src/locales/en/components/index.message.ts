import Component from "~/locales/interfaces/components/index.interface";
import accountSelector from "./account-selector.message";

const components: Component = {
  accountSelector,
};

export default components;
